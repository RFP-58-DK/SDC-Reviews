//Create your own config.js file that is gitIgnored and insert your own postgresql credentials

const config = {
  user: 'postgreSQL username',
  password: 'postgreSQL user password',
  database: 'database name'
}

module.exports = config;